                                                     SQL 문법 정리

1. 테이블 생성 및 조회
   create table B(
         column1 datatype1,
         column2 datatype2,
         column3 datatype3
   );
 
  show tables;
  show columns in person;


2. 데이터 삽입
   insert into B(column1, column2, column3)
   values(value1, value2, value3);


3. 데이터 조회
   select * from B; // B테이블에서 모든 칼럼을 조회해라
   select A from B; // B테이블에서 A칼럼을 조회해라
   select A, C from B; 
   select A from B where 조건1;
   select * from B where 조건1 and 조건2;
 and / or / not 