# 26-03-24

## Visual Studio Code

## 오늘자 주요 태그
    노드CRUD
    
## 오늘 배운 것

    DAY 13 JS 참조 (4,5,6번 도움됨)

    *** 전반적인 정리 ***
    document.getElementById() : get으로 id를 찾을때는 #을 사용하지않음
    document.querySelector() : 선택자! 어떤걸 찾는지 표기해줘야함 ('.','#' 등)
            .querySelectorAll() : 모든 것을 선택할 때 사용


    ** 여기 중요! **
    liEls = Object.entries(liEls) : 배열이 아닌 것을 길이2의 배열로 만듬.
    liEls = Array.from(liEls) : Array! (중요) 유사배열의 배열화, 배열함수 사용가능


    *** form 찾기! ***
    const forms = document.forms; 
    console.log("forms",forms);       : 전체 form를 찾는다.
    console.log("forms[0]",forms[0]); : 개별로 하나씩 찾는다.
    console.log('forms[0][0]',forms[0][0]); : 개별 form 내부에 input에 접근!

    
    *** 개별 form 내의 input 접근(name 부여) ***
    const joinForm = document.joinForm; : joinForm 찾기! ( form에 name="joinForm" 부여해 빠르게 찾음)
    joinForm.username.value = '홍길동'; : username이름의 input에 '홍길동'을 부여!
    joinForm.nickname.value = '티모';  : nickname이름의 input에 '티모'를 부여!


    *** 스크립트로 클래스 부여 및 이벤트 속성 추가! ***
    const btnEls = document.querySelectorAll('.btn');  : .btn클래스 모두 찾기!
    btnEls.forEach((btnEl,idx)=>{    : 
      console.log(idx,btnEl);
      btnEl.addEventListener('mouseover',(e)=>{btnEl.classList.add(`btn--style${idx+1}`)});
            : 마우스가 안으로 들어올때 btn클래스에 btn--style$ 클래스 추가!
      btnEl.addEventListener('mouseleave',(e)=>{btnEl.classList.remove(`btn--style${idx+1}`)});
            : 마우스가 나갈때 btn클래스에 btn--style$ 클래스 제거!
    });


    *** 이벤트 처리의 흐름!!(중요) ***
    1. 키입력에 따른 이벤트 처리
    insertValueEl.addEventListener("keydown", (e) => {
      console.log(e.keyCode);             
      if (e.keyCode == 13) {                  //1. 엔터키를 친다면
        const value = insertValueEl.value;    //2. insertValueEl.value 값 가져오기
        if (!isValid(value))                  //3. 유효성검증
          return;

        createNode(value);                    //4. 새노드생성 -> Container연결
        insertValueEl.value = '';             //5. insertValueEl 비우기
      }
    });
    
    2. 클릭에 따른 이벤트 처리
    btnInsertEl.addEventListener("click", (e) => {
      const value = insertValueEl.value; //1. insertValueEl.value 값 가져오기
      if (!isValid(value))               //2. 유효성검증
        return;
      createNode(value);                 //3. 새노드생성 -> Container연결
      insertValueEl.value = '';          //4. insertValueEl 비우기
    });
    
    3. 유효성 검사!!
      function isValid(value){
          return false;
      }

    4. 그 이후
    //생성 함수 만들기
      function createNode()
    //새 노드생성
      const itemEl = document.createElement('div');
      const leftEl = document.createElement('div');
      const inputEl = document.createElement('input');
      const rightEl = document.createElement('div');
      const aEl = document.createElement('a');
    //클래스 추가
      itemEl.classList.add('item');
      leftEl.className = 'left';
      rightEl.className = 'right';
    //속성 추가
      inputEl.setAttribute('readonly', true);
      aEl.setAttribute('href', 'javascript:void(0)');
    //데이터 삽입
      aEl.innerHTML = "삭제";
      inputEl.value = text;
    //이벤트 처리(삭제버튼)
      aEl.addEventListener('click',(e)=>{
        // console.log(e.target.parentNode.parentNode);
        const itemEl = e.target.parentNode.parentNode;
        itemEl.remove();
      })
    //노드간 연결
      leftEl.appendChild(inputEl);
      rightEl.appendChild(aEl);
      itemEl.appendChild(leftEl);
      itemEl.appendChild(rightEl);
      containerEl.prepend(itemEl);
      










    
